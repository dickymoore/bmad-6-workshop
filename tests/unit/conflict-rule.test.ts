// Placeholder unit tests (Vitest not installed). Enable when vitest is added.
describe.skip('Conflict rule (user + date)', () => {
  it('[P0] blocks second booking for same user/date', () => {
    // TODO: import conflict validator and assert it rejects duplicates.
  });
});

