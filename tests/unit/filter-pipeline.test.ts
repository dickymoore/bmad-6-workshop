// Placeholder unit tests (Vitest not installed). Enable when vitest is added.
describe.skip('Filter pipeline (availability)', () => {
  it('[P0] filters by office/floor/date', () => {
    // TODO: import filter function and assert filtering logic.
  });
});

